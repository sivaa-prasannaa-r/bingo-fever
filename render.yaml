services:
  - type: web
    name: bingo-fever-server
    runtime: node
    rootDir: server
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: PORT
        value: 3001
    healthCheckPath: /
